MONGO_URI=mongodb://127.0.0.1:27017/expense-tracker
PORT=8080
CORS_ORIGINS=http://localhost:5173

# Configure both values temporarily only when creating a database user.
# BOOTSTRAP_USERNAME=admin
# BOOTSTRAP_PASSWORD=replace-with-12-to-256-characters

# Optional account recovery: rename an existing user during bootstrap.
# BOOTSTRAP_REPLACE_USERNAME=old-username
